{% import "../../../shared/textBox/textBox.twig" as textbox %}
<section class="page-section">
		<div class="container">
			<div class="row">
				{% for item in textBoxSection.content %}
					<div class="col-xs-11 col-sm-5  col-lg-3">
						{{textbox.render(item)}}
					</div>
				{% endfor %}

			</div>
		</div>
</section>
